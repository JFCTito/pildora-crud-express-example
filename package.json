{
  "name": "pildora-express-typescript-doing-crud",
  "version": "1.0.0",
  "description": "Hola, mi nombre es Jesus Fajardo y aqui deesta toda la informacion referente a mi pildora de \"CRUD con Express y TypeScript\".",
  "main": "Instructions.js",
  "scripts": {
    "build": "rimraf dist && npx tsc",
    "prestart": "npm run build",
    "start": "node dist/index.js",
    "preserve": "npm run build",
    "serve": "concurrently \"npx tsc -w\" \"nodemon dist/index.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.2.5",
    "concurrently": "^8.0.1",
    "express": "^4.18.2",
    "rimraf": "^5.0.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
